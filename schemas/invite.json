{
  "id": "invite.json",
  "definitions": {
    "model": {
      "type": "object",
      "additionalProperties": false,
      "required": ["organizationId", "userId", "token"],
      "properties": {
        "organizationId": {
          "$ref": "organization.json#/definitions/model/properties/id"
        },
        "userId": {
          "$ref": "common.json#/definitions/userId"
        },
        "token": {
          "type": "string",
          "description": "Invite token"
        }
      }
    }
  }
}
